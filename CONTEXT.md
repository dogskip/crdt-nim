# crdt-nim

수렴하는 복제 자료구조(CRDT)를 Nim 으로 구현한 라이브러리.
이 문서는 이 저장소에서만 쓰는 말을 정한다.

## 언어

**CRDT**:
조정 없이 병합해도 모든 복제본이 같은 상태로 모이는 자료구조.
_Avoid_: 분산 자료구조, 동기화 자료구조

**복제본 (replica)**:
같은 자료구조를 따로 들고 있는 한 벌.

**병합 (merge)**:
두 복제본의 상태를 하나로 합치는 연산. 순서와 횟수에 무관하게 같은 결과여야 한다.
_Avoid_: 동기화, 충돌 해결

**수렴 (convergence)**:
같은 연산을 받은 복제본이 모두 같은 상태가 되는 성질.

**노드 (NodeId)**:
복제본을 구분하는 이름. 같은 무리 안에서 겹치면 안 된다.
_Avoid_: 피어, 서버, 클라이언트

**타임스탬프 (Timestamp)**:
값의 최신성을 견주는 논리 시각. 실제 시각일 필요는 없다.
_Avoid_: 시각, 시간

**tombstone**:
제거된 항목이 되살아나지 않도록 남겨 두는 표시.
_Avoid_: 삭제 표시, 묘비

**관찰 (observed)**:
복제본이 어떤 항목을 실제로 받아 본 상태. OR-Map 의 제거는 관찰한 항목에만 적용된다.

**동시 (concurrent)**:
두 사건 사이에 인과 관계가 없는 상태. 벡터 클럭이 판별한다.
_Avoid_: 병렬

**항목 ID (EntryId)**:
OR-Map 에서 항목 하나를 가리키는 이름. 발급한 노드와 그 노드의 발급 순번으로 이루어진다.

## 자료구조

**G-Set**: 추가만 가능한 집합. 병합은 합집합이다.

**2P-Set**: 추가 집합과 제거 집합을 함께 두는 집합. 제거는 되돌릴 수 없다.

**LWW-Set**: 원소마다 마지막 갱신 시각을 두고 더 나중 것을 채택하는 집합.

**OR-Map**: 키마다 고유 ID 를 가진 항목을 두는 맵. 제거는 관찰한 ID 에만 적용된다.

**벡터 클럭 (Vector Clock)**: 노드마다 사건 번호를 두고 두 시계의 인과 관계를 판별하는 자료구조.
